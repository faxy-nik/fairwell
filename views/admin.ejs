<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin - Class Memories</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="admin-page">
  <div class="container">
    <header class="admin-header">
      <h1>Admin Panel</h1>
      <nav>
        <a href="/" target="_blank">View Public Page</a>
        <a href="/admin/logout">Logout</a>
      </nav>
    </header>

    <% if (typeof msg !== 'undefined' && msg) { %>
      <div class="alert success"><%= msg %></div>
    <% } %>
    <% if (typeof err !== 'undefined' && err) { %>
      <div class="alert error"><%= err %></div>
    <% } %>

    <section class="admin-section">
      <h2>Settings</h2>
      <form method="POST" action="/admin/change-password" class="inline-form">
        <input type="password" name="currentPassword" placeholder="Current password" required>
        <input type="password" name="newPassword" placeholder="New password" required>
        <button type="submit" class="btn small">Change Password</button>
      </form>
    </section>

    <section class="admin-section">
      <h2>General Roadmap URLs</h2>
      <p class="empty-note">Paste resource links for the 7-week plan and 9 AI skills. Students will see these as clickable links.</p>
      <form method="POST" action="/api/general-urls" class="urls-form">
        <div class="urls-grid">
          <div class="url-group">
            <h4>7-Week Plan</h4>
            <% var weekLabels = {week1:'Week 1 — Python Basics',week2:'Week 2 — AI APIs',week3:'Week 3 — Vibe Coding',week4:'Week 4 — Build a Project',week5:'Week 5 — HTML/CSS + v0.dev',week6:'Week 6 — Deploy Live',week7:'Week 7 — Polish & Portfolio'}; %>
            <% Object.keys(weekLabels).forEach(function(k) { %>
              <label><%= weekLabels[k] %><input type="url" name="<%= k %>" value="<%= generalUrls[k] || '' %>" placeholder="https://..."></label>
            <% }); %>
          </div>
          <div class="url-group">
            <h4>9 AI Skills</h4>
            <% var skillLabels = {skill1:'Prompt Engineering',skill2:'AI Workflow Automation',skill3:'AI Agents',skill4:'RAG',skill5:'Fine-Tuning & GPTs',skill6:'Multimodal AI',skill7:'AI Video Generation',skill8:'AI Tool Stacking',skill9:'LLM Evaluation'}; %>
            <% Object.keys(skillLabels).forEach(function(k) { %>
              <label><%= skillLabels[k] %><input type="url" name="<%= k %>" value="<%= generalUrls[k] || '' %>" placeholder="https://..."></label>
            <% }); %>
          </div>
        </div>
        <button type="submit" class="btn">Save URLs</button>
      </form>
    </section>

    <section class="admin-section">
      <h2>Add Person</h2>
      <form method="POST" action="/api/person" class="inline-form">
        <input type="text" name="name" placeholder="Person name" required>
        <button type="submit" class="btn">Add</button>
      </form>
    </section>

    <% people.forEach(function(person) { %>
      <section class="admin-person">
        <div class="person-head">
          <h2><%= person.name %></h2>
          <div class="person-actions">
            <form method="POST" action="/api/person/<%= person.id %>/rename" class="inline-form">
              <input type="text" name="name" placeholder="Rename" required>
              <button type="submit" class="btn small">Rename</button>
            </form>
            <form method="POST" action="/api/person/<%= person.id %>/delete" onsubmit="return confirm('Delete this person and all their content?')">
              <button type="submit" class="btn small danger">Delete</button>
            </form>
          </div>
        </div>
        <div class="access-link">
          <strong>Access Link:</strong>
          <input type="text" value="<%= publicUrl %>/p/<%= person.accessToken %>" readonly onclick="this.select()" class="link-input">
          <button class="btn small" onclick="navigator.clipboard.writeText(this.previousElementSibling.value);this.textContent='Copied!';setTimeout(()=>this.textContent='Copy',1500)">Copy</button>
        </div>

        <div class="add-section">
          <h3>Add Section to <%= person.name %></h3>
          <form method="POST" action="/api/person/<%= person.id %>/section" class="inline-form">
            <input type="text" name="title" placeholder="Section title" required>
            <select name="type">
              <option value="images">Images</option>
              <option value="voice">Voice</option>
              <option value="videos">Videos</option>
              <option value="texts">Texts</option>
              <option value="other">Other</option>
            </select>
            <button type="submit" class="btn small">Add</button>
          </form>
        </div>

        <div class="admin-section-box roadmap-box">
          <div class="section-head">
            <h3>Roadmap</h3>
          </div>
          <div class="upload-form">
            <h4>Add Milestone</h4>
            <form method="POST" action="/api/person/<%= person.id %>/roadmap/add" class="upload-form-inner">
              <input type="text" name="title" placeholder="Milestone title" required>
              <input type="date" name="date">
              <input type="text" name="description" placeholder="Description">
              <button type="submit" class="btn small">Add</button>
            </form>
          </div>
          <% if (person.roadmap && person.roadmap.length > 0) { %>
            <div class="items-list">
              <h4>Milestones (<%= person.roadmap.length %>)</h4>
              <table>
                <thead>
                  <tr>
                    <th>Date</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% person.roadmap.forEach(function(item, idx) { %>
                    <tr>
                      <td><%= item.date %></td>
                      <td><%= item.title %></td>
                      <td><%= item.description %></td>
                      <td>
                        <form method="POST" action="/api/person/<%= person.id %>/roadmap/delete" onsubmit="return confirm('Delete this milestone?')">
                          <input type="hidden" name="index" value="<%= idx %>">
                          <button type="submit" class="btn small danger">Delete</button>
                        </form>
                      </td>
                    </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>
          <% } else { %>
            <p class="empty-note">No milestones yet. Add the key moments for <%= person.name %>!</p>
          <% } %>
        </div>

        <% person.sections.forEach(function(section) { %>
          <div class="admin-section-box">
            <div class="section-head">
              <h3><%= section.title %> <span class="badge"><%= section.type %></span></h3>
              <form method="POST" action="/api/person/<%= person.id %>/section/<%= section.id %>/delete" onsubmit="return confirm('Delete this section and all its files?')">
                <button type="submit" class="btn small danger">Delete Section</button>
              </form>
            </div>

            <div class="upload-form">
              <h4>Upload to <%= section.title %></h4>
              <form method="POST" action="/api/upload" enctype="multipart/form-data" class="upload-form-inner">
                <input type="hidden" name="personId" value="<%= person.id %>">
                <input type="hidden" name="sectionId" value="<%= section.id %>">
                <input type="file" name="file" required>
                <input type="text" name="title" placeholder="Display title (optional)">
                <label>
                  Release Date:
                  <input type="date" name="releaseDate" value="<%= new Date().toISOString().split('T')[0] %>">
                </label>
                <button type="submit" class="btn small">Upload</button>
              </form>
            </div>

            <% if (section.items.length > 0) { %>
              <div class="items-list">
                <h4>Items (<%= section.items.length %>)</h4>
                <table>
                  <thead>
                    <tr>
                      <th>Title</th>
                      <th>File</th>
                      <th>Release Date</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% section.items.forEach(function(item) { %>
                      <tr>
                        <td><%= item.title %></td>
                        <td><%= item.originalName %></td>
                        <td><%= new Date(item.releaseDate).toLocaleDateString() %></td>
                        <td>
                          <form method="POST" action="/api/person/<%= person.id %>/section/<%= section.id %>/item/delete" onsubmit="return confirm('Delete this item?')">
                            <input type="hidden" name="filename" value="<%= item.filename %>">
                            <button type="submit" class="btn small danger">Delete</button>
                          </form>
                        </td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <p class="empty-note">No items in this section</p>
            <% } %>
          </div>
        <% }); %>
      </section>
    <% }); %>
  </div>
</body>
</html>
